// Pulls in the ambient `declare module "cloudflare:test"` types shipped by
// @cloudflare/vitest-pool-workers so the test files can import from it.
/// <reference types="@cloudflare/vitest-pool-workers/types" />
